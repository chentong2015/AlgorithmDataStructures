TODO. 数据压缩思想
在数据遍历时压缩数据，使用压缩的数据得出结果 !!
压缩消除算法: 组合相消的两个i和j位置是任意的 !!

TODO. 数据压缩实战运用
Case 1: 压缩数据 + 动态编程(累计历史) + 多次遍历
  5, 3, 3, 3, 5, 6, 2
  0  1  2  3  0  0  1 = Decrease 前面下降数据的统计
  0  4  3  2  1  0  0 = Increase 后面上升数据的统计

Case 2: 压缩数据，压缩数据进行统计
- 如果当前位置为"0": 只关心前面有多少"01" -> "0"可以来自前面任何位置
- 如果当前位置为"1": 只关心前面有多少"10" -> 前面的"1"全部可以缩合

Case 3: Sliding Windows Max
将遍历的数据压缩到LinkedList<>存储空间，用于计算结果
- 链表存储数据坐标位置，减少存储空间
- 链表存储时剔除无用的前面小数据，减少存储的长度