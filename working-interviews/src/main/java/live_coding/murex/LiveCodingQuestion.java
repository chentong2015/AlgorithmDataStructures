package live_coding.murex;

// 一道Easy简单 + 一道Tree树结构
public class LiveCodingQuestion {

    // TODO. 二叉树中最小的公共前继
    // LCA(Lowest Common Ancestor) of a Binary Tree
    // 输入二叉树的root结点，以及两个结点的值，返回最小公共node
    public static int findLCA(TreeNode root, int p, int q) {
        // 判断初始条件
        // 递归判断左右子树
        // 按照找到的结果进行返回
        return 0;
    }

    class TreeNode {
        int val;
        TreeNode left;
        TreeNode right;
    }
}
